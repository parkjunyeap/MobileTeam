import { StyleSheet, Text, View } from "react-native";

const MyReviewInfo = () => {
  return (
    <View style={styles.container}>
      <Text>MyReviewInfo</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default MyReviewInfo;
